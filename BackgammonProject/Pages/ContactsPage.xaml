<Page
    x:Class="BackgammonProject.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BackgammonProject"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    DataContext="{Binding Source={StaticResource Locator},Path=ContactsVM}">

    <Grid Background="White">
        
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="2*"/>
        </Grid.RowDefinitions>

        <Button Name="Menu" Content="▼" FontSize="60" VerticalAlignment="Top" Grid.Row="0"/>
        <TextBlock Name="Title" Text="Shlook TalkBack" FontSize="60" HorizontalAlignment="Center" Grid.Row="0"/>

        <StackPanel Grid.Row="1" HorizontalAlignment="Center" Spacing="50">
            <TextBlock Name="userName" Text="Temporary UserName" FontSize="70"/>
            <StackPanel Orientation="Horizontal" Spacing="100" HorizontalAlignment="Center">
                <Button Name="chatBtn" Content="chat image" FontSize="40"/>
                <Button Name="gameBtn" Content="game image" FontSize="40"/>
            </StackPanel>
        </StackPanel>

        <StackPanel Grid.Row="2">
            <TextBlock Name="onlineContacts" Text="Online contacts:" FontSize="50"/>
            <ListBox Name="onlineList" ItemTemplate="{StaticResource ContactsDataTemplate}"/>
        </StackPanel>

        <StackPanel Grid.Row="3">
            <TextBlock Name="offlineContacts" Text="Offline contacts:" FontSize="50"/>
            <ListBox Name="offlineList" ItemTemplate="{StaticResource ContactsDataTemplate}"/>
        </StackPanel>
        
    </Grid>
</Page>
